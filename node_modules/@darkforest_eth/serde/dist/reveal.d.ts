import type { DarkForestCore } from '@darkforest_eth/contracts/typechain';
import type { Awaited, RevealedCoords } from '@darkforest_eth/types';
export declare type RawRevealedCoords = Awaited<ReturnType<DarkForestCore['revealedCoords']>>;
/**
 * Converts the result of a typechain-typed ethers.js contract call returning a
 * `DarkForestTypes.RevealedCoords` struct into a `RevealedCoords` object (see
 * @darkforest_eth/types)
 *
 * @param rawRevealedCoords the result of a typechain-typed ethers.js contract
 * call returning a DarkForestTypes.RevealedCoords` struct
 */
export declare function decodeRevealedCoords(rawRevealedCoords: RawRevealedCoords): RevealedCoords;
